name: Generate PepMAX vials (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy generator disabled
        run: |
          echo "Legacy automatic SVG vial generation is disabled."
          echo "Use the approved SKU/variant image workflow instead."
