const STORE_COPY={
  es:{research:'EXCLUSIVO PARA INVESTIGACIÓN DE LABORATORIO',shop:'Shop',catalog:'Catálogo',search:'Buscar por nombre, presentación o SKU',featured:'Catálogo de investigación',featuredSub:'Explora compuestos, selecciona una presentación y añade el artículo al carrito.',all:'Todos los productos',price:'Precio',add:'Añadir',quick:'Vista rápida',cart:'Carrito',empty:'No se encontraron productos.',researchDesc:'Material de referencia catalogado para investigación de laboratorio.',view:'Ver ficha técnica',close:'Cerrar',total:'Total',clear:'Vaciar carrito',sortDefault:'Orden del catálogo',sortAZ:'Nombre A–Z',sortPrice:'Precio menor',availability:'Disponibilidad sujeta a verificación regulatoria y comercial.'},
  pt:{research:'EXCLUSIVO PARA PESQUISA LABORATORIAL',shop:'Shop',catalog:'Catálogo',search:'Buscar por nome, apresentação ou SKU',featured:'Catálogo de pesquisa',featuredSub:'Explore compostos, selecione uma apresentação e adicione o item ao carrinho.',all:'Todos os produtos',price:'Preço',add:'Adicionar',quick:'Visão rápida',cart:'Carrinho',empty:'Nenhum produto encontrado.',researchDesc:'Material de referência catalogado para pesquisa laboratorial.',view:'Ver ficha técnica',close:'Fechar',total:'Total',clear:'Limpar carrinho',sortDefault:'Ordem do catálogo',sortAZ:'Nome A–Z',sortPrice:'Menor preço',availability:'Disponibilidade sujeita à verificação regulatória e comercial.'},
  en:{research:'FOR LABORATORY RESEARCH ONLY',shop:'Shop',catalog:'Catalog',search:'Search by name, presentation or SKU',featured:'Research catalog',featuredSub:'Explore compounds, select a presentation and add the item to cart.',all:'All products',price:'Price',add:'Add to cart',quick:'Quick view',cart:'Cart',empty:'No products found.',researchDesc:'Cataloged reference material for laboratory research.',view:'View technical page',close:'Close',total:'Total',clear:'Clear cart',sortDefault:'Catalog order',sortAZ:'Name A–Z',sortPrice:'Lowest price',availability:'Availability is subject to regulatory and commercial verification.'}
};
function storeLocale(){return document.documentElement.lang==='pt-BR'?'pt':document.documentElement.lang==='en'?'en':'es'}
function storePrefix(){const l=storeLocale();return l==='es'?'':`../`}
function productBase(){const l=storeLocale();return l==='es'?'../peptides/':'../../peptides/'}
function imageFor(p,item){const dose=(item?.[1]||'').toLowerCase().replace(/\s+/g,'-').replace(/[^a-z0-9.-]/g,'');return `${productBase()}images/${p.slug}-${dose}.webp`}
function fallbackImage(el,p){el.onerror=null;el.src=`${productBase()}images/${p.slug}.webp`;el.onerror=()=>{el.style.display='none'}}
function cartRead(){try{return JSON.parse(localStorage.getItem('pepmax-cart')||'[]')}catch{return[]}}
function cartWrite(v){localStorage.setItem('pepmax-cart',JSON.stringify(v));renderCartCount();renderCart()}
function addCart(p,item){const cart=cartRead();const key=`${p.slug}|${item[0]}|${item[1]}`;const found=cart.find(x=>x.key===key);if(found)found.qty+=1;else cart.push({key,slug:p.slug,name:p.name,sku:item[0],presentation:item[1],price:item[2],qty:1});cartWrite(cart);document.querySelector('.cart-drawer')?.classList.add('open')}
function renderCartCount(){const n=cartRead().reduce((a,b)=>a+b.qty,0);document.querySelectorAll('[data-cart-count]').forEach(x=>x.textContent=n)}
function renderCart(){const c=STORE_COPY[storeLocale()],box=document.querySelector('[data-cart-items]');if(!box)return;const items=cartRead();box.innerHTML=items.length?items.map((x,i)=>`<div class="cart-item"><div class="cart-item-row"><b>${x.name}</b><button class="close" aria-label="${c.close}" onclick="removeCart(${i})">×</button></div><small>${x.presentation} · ${x.sku==='—'?'SKU —':`SKU ${x.sku}`}</small><div class="cart-item-row" style="margin-top:8px"><span>${x.qty} × $${Number(x.price).toFixed(2)}</span><b>$${(x.qty*x.price).toFixed(2)}</b></div></div>`).join(''):`<div class="empty">${c.empty}</div>`;const total=items.reduce((a,b)=>a+b.qty*b.price,0);const t=document.querySelector('[data-cart-total]');if(t)t.textContent=`$${total.toFixed(2)}`}
function removeCart(i){const c=cartRead();c.splice(i,1);cartWrite(c)}
function clearCart(){cartWrite([])}
function openCart(){document.querySelector('.cart-drawer')?.classList.add('open')}
function closeCart(){document.querySelector('.cart-drawer')?.classList.remove('open')}
function productCard(p){const c=STORE_COPY[storeLocale()],first=p.items[0],opts=p.items.map((i,idx)=>`<option value="${idx}">${i[1]} · $${Number(i[2]).toFixed(2)}</option>`).join('');return `<article class="product-card" data-name="${p.name.toLowerCase()}" data-search="${[p.name,...p.items.flat()].join(' ').toLowerCase()}"><a class="product-image" href="${productBase()}${p.slug}/"><img src="${imageFor(p,first)}" alt="${p.name} ${first[1]}" loading="lazy" onerror="fallbackImage(this,PRODUCTS.find(x=>x.slug==='${p.slug}'))"></a><div class="product-body"><div class="product-kicker">${c.research}</div><div class="product-name">${p.name}</div><div class="product-desc">${c.researchDesc}</div><div class="variant-row"><select class="variant-select" aria-label="Presentation" onchange="updateVariant(this,'${p.slug}')">${opts}</select><span class="sku" data-sku>${first[0]==='—'?'SKU —':`SKU ${first[0]}`}</span></div><div class="price-row"><span class="price" data-price>$${Number(first[2]).toFixed(2)}</span></div><div class="card-actions"><button class="add" onclick="addFromCard(this,'${p.slug}')">${c.add}</button><button class="quick" onclick="quickView('${p.slug}')">${c.quick}</button></div></div></article>`}
function updateVariant(sel,slug){const p=PRODUCTS.find(x=>x.slug===slug),item=p.items[Number(sel.value)],card=sel.closest('.product-card');card.querySelector('[data-price]').textContent=`$${Number(item[2]).toFixed(2)}`;card.querySelector('[data-sku]').textContent=item[0]==='—'?'SKU —':`SKU ${item[0]}`;const img=card.querySelector('img');img.src=imageFor(p,item)}
function addFromCard(btn,slug){const p=PRODUCTS.find(x=>x.slug===slug),sel=btn.closest('.product-card').querySelector('.variant-select');addCart(p,p.items[Number(sel.value)])}
function quickView(slug){const c=STORE_COPY[storeLocale()],p=PRODUCTS.find(x=>x.slug===slug),item=p.items[0],m=document.querySelector('.modal');m.querySelector('[data-modal-title]').textContent=p.name;m.querySelector('[data-modal-copy]').textContent=c.researchDesc;m.querySelector('[data-modal-link]').href=`${productBase()}${p.slug}/`;m.querySelector('[data-modal-link]').textContent=c.view;m.querySelector('[data-modal-image]').src=imageFor(p,item);m.querySelector('[data-modal-image]').onerror=function(){fallbackImage(this,p)};m.classList.add('open')}
function closeModal(){document.querySelector('.modal')?.classList.remove('open')}
function applySearch(){const q=(document.querySelector('#storeSearch')?.value||'').trim().toLowerCase();document.querySelectorAll('.product-card').forEach(card=>card.hidden=q&&!card.dataset.search.includes(q))}
function applySort(){const grid=document.querySelector('#storeGrid'),mode=document.querySelector('#storeSort')?.value;if(!grid)return;const cards=[...grid.children];if(mode==='az')cards.sort((a,b)=>a.dataset.name.localeCompare(b.dataset.name));if(mode==='price')cards.sort((a,b)=>Number(a.querySelector('[data-price]').textContent.replace('$',''))-Number(b.querySelector('[data-price]').textContent.replace('$','')));cards.forEach(x=>grid.appendChild(x))}
function initStore(){const c=STORE_COPY[storeLocale()],grid=document.querySelector('#storeGrid');if(grid&&typeof PRODUCTS!=='undefined')grid.innerHTML=PRODUCTS.map(productCard).join('');document.querySelector('#storeSearch')?.addEventListener('input',applySearch);document.querySelector('#storeSort')?.addEventListener('change',applySort);renderCartCount();renderCart();document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeModal();closeCart()}})}
document.addEventListener('DOMContentLoaded',initStore);
